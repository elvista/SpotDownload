# =============================================================================
# CrateDigger — single repo-root .env for FastAPI + Vite.
# Register the Spotify redirect URI in the Developer Dashboard (must match exactly):
#   - http://127.0.0.1:5174/api/auth/spotify/callback   (production deploy, :5174)
#   - http://127.0.0.1:8000/api/auth/spotify/callback   (dev via `npm run dev`, :8000)
# Register whichever you use; register both if you use both.
# =============================================================================

# --- Spotify (shared app credentials) ---
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here
# Must use 127.0.0.1 (Spotify rejects http://localhost for redirect URIs). Match exactly in the Dashboard.
SPOTIFY_REDIRECT_URI=http://127.0.0.1:5174/api/auth/spotify/callback
# Where to send the browser after Spotify login (same-origin deploy: match SPOTIFY_REDIRECT_URI host:port)
FRONTEND_ORIGIN=http://127.0.0.1:5174
DOWNLOAD_PATH=~/Music/CrateDigger
MONITOR_INTERVAL_MINUTES=30
# Parallel yt-dlp downloads (1–8; default 3). Higher values can help on fast disks but may increase throttling.
# DOWNLOAD_CONCURRENCY=3
# Optional: encrypt stored Spotify tokens at rest
# ENCRYPTION_KEY=

# --- Genre ID (genre classification) ---
LASTFM_API_KEY=your_lastfm_api_key_here
# Claude API is used as fallback when Last.fm can't identify a track
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# --- Mixtape ID (fingerprinting — required for analysis) ---
ACRCLOUD_ACCESS_KEY=your_acrcloud_access_key
ACRCLOUD_ACCESS_SECRET=your_acrcloud_access_secret
ACRCLOUD_HOST=identify-us-west-2.acrcloud.com
AUDD_API_TOKEN=your_audd_api_token

# Mixtape → Spotify playlists reuse the same OAuth as Spotify ID Settings — no extra env vars needed.
