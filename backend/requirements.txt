fastapi
python-multipart
uvicorn[standard]
sqlalchemy
spotipy
python-dotenv
apscheduler
sse-starlette
aiosqlite
pydantic-settings
mutagen

# Testing
pytest
pytest-asyncio
httpx

# Resilience
tenacity

# Migrations
alembic

# AI
anthropic

# Security
cryptography

# Upscale section — record-pool scrapers (DJCity, zipDJ, BPM Supreme)
# After `pip install -r requirements.txt`, run `playwright install chromium`
# once to fetch the browser binary. Pool scraping is gated by the env var
# UPSCALE_POOLS_ENABLED=1 so a fresh install with no browser still boots.
playwright
